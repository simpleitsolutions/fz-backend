{% if admin.hasAccess('edit', object) and admin.hasRoute('edit') %}
    <a href="{{ path('security_user_edit', {'id':object.id}) }}" class="btn btn-sm btn-default edit_link" title="{{ 'action_edit'|trans({}, 'SonataAdminBundle') }}">
        <i class="fa fa-pencil" aria-hidden="true"></i>
    </a>
{% endif %}
