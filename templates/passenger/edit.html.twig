<div class="col-xs-5 col-sm-5 col-md-4">{{ form_row(form.name, {'label':false}) }}</div>
{% if form.flight is defined %}
<div class="hidden-xs hidden-sm col-md-4">{{ form_row(form.flight, {'label':false}) }}</div>
{% endif %}
{% if form.pilot is defined %}
<div class="col-xs-5 col-sm-5 col-md-3">{{ form_row(form.pilot, {'label':false}) }}</div>
{% endif %}
