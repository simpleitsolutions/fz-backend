{% if admin.hasAccess('show', object) and admin.hasRoute('show') %}
    <a href="{{ path('voucher_custom_show', {'id':object.id}) }}" class="btn btn-sm btn-default view_link" title="{{ 'action_show'|trans({}, 'SonataAdminBundle') }}">
        <i class="fa fa-eye" aria-hidden="true"></i>
    </a>
{% endif %}
